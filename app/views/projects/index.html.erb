<% if current_user %>
  <% @projects.each do |project| %>
    <h2><%= link_to project.name, project_path(project) %></h2>
  <% end %>
<% else %>
  <h2>There are no projects to display. Please signup or signin to create and display projects.</h2>
<% end %>
